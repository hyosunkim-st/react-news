// import React from 'react';
// import  { connect } from 'react-redux';
// import TodoContext from './TodoContext';
// import MenuContext from './Context';

// const CounterContainer = ({}) => {
//     return (
//         <CounterContainer />
//     );
// };

// const mapStateToProps = (state) => {
//     return {
//             articles: state.Menu[country][category]
             
//     };
// };

// const mapDispatchToProps = (dispatch) => {
//     return{
//         Menu : (menu) => { dispatch(action.setMenu(menu)) },
//         Todo : (todo) => { dispatch(action.setTodo(todo)) }
//     };
// };
// export default connect(mapStateToProps, mapDispatchToProps )(CounterContainer);  //connect함수를 반환하고 그 파라미터로 Counter